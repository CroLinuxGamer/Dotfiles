#!/bin/dash
# change it to #!/bin/sh if you are on debian based distro

status=$(mpc status | sed -n '/playing/p' | cut -c2-8 | sed 's/^[ \t]*//')

if [ "$status" = "playing" ]; then # If mpd is playing
    printf ""
else
    printf "契"
fi

