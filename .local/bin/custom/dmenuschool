#!/bin/sh

# feed the script with your shool related files path
# and it will list them all and open what you choose
# with your preferred program using xdg-open

find "$1" | dmenu -l 30 | xargs -I {} xdg-open "{}"
